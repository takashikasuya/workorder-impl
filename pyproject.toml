[project]
name = "workorder-impl"
version = "0.1.0"
requires-python = ">=3.12"

[tool.uv.workspace]
members = ["shared", "services/*"]

[dependency-groups]
dev = ["ruff>=0.4", "pyyaml>=6.0"]

[tool.ruff]
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "I"]
