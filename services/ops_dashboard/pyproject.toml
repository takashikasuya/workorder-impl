[project]
name = "gutp-ops-dashboard"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "httpx>=0.27",
    "gutp-shared",
]

[tool.uv.sources]
gutp-shared = { workspace = true }
