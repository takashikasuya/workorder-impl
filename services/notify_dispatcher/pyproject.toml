[project]
name = "gutp-notify-dispatcher"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "nats-py>=2.9",
    "httpx>=0.27",
    "gutp-shared",
]

[tool.uv.sources]
gutp-shared = { workspace = true }
